node_modules
.cache
build
/public/build
/app/build

# Never bake secrets or local state into the image
.env
.env.*
.shopify
.git
.gitignore

# Leftover local SQLite file. The app uses Postgres (Neon) everywhere now, so
# this is dead weight — never bake it into the image.
*.sqlite
*.sqlite-journal
prisma/dev.sqlite
