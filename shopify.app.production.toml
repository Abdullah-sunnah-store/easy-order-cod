# Release configuration — the URLs that ship to the App Store.
#
# `shopify app dev` rewrites the URL fields of whichever config is active, so
# keep development on shopify.app.toml and release from this file:
#
#   shopify app config use production
#   shopify app deploy
#   shopify app config use shopify.app.toml   # back to dev
#
# automatically_update_urls_on_dev is false here so a stray `dev` run can never
# point the published app at a Cloudflare tunnel.

client_id = "7d6846fb9f944b6701f6cc35b77eab6f"
name = "Easy order COD"
application_url = "https://easy-order-cod.onrender.com/app"
embedded = true
handle = "easy-order-cod"

[build]
automatically_update_urls_on_dev = false

# The storefront form calls /apps/cod/settings and /apps/cod/order through this
# proxy. Without it those paths 404 and no COD order can ever be created.
[app_proxy]
url = "https://easy-order-cod.onrender.com/proxy"
subpath = "cod"
prefix = "apps"

[webhooks]
api_version = "2026-07"

  # App lifecycle
  [[webhooks.subscriptions]]
  topics = [ "app/uninstalled" ]
  uri = "/webhooks/app/uninstalled"

  [[webhooks.subscriptions]]
  topics = [ "app/scopes_update" ]
  uri = "/webhooks/app/scopes_update"

  # Mandatory GDPR / privacy compliance webhooks (required for App Store apps)
  [[webhooks.subscriptions]]
  compliance_topics = [ "customers/data_request" ]
  uri = "/webhooks/customers/data_request"

  [[webhooks.subscriptions]]
  compliance_topics = [ "customers/redact" ]
  uri = "/webhooks/customers/redact"

  [[webhooks.subscriptions]]
  compliance_topics = [ "shop/redact" ]
  uri = "/webhooks/shop/redact"

[access_scopes]
# Only what the COD app actually uses: create orders, read orders/products, and
# read the shop's shipping zones for the Form builder's auto-synced rates.
scopes = "read_orders,write_orders,read_products,read_shipping"

[auth]
redirect_urls = [
  "https://easy-order-cod.onrender.com/auth/callback",
  "https://easy-order-cod.onrender.com/auth/shopify/callback",
  "https://easy-order-cod.onrender.com/api/auth/callback"
]

[pos]
embedded = false
